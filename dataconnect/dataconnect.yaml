specVersion: "v1beta"
serviceId: "ssc-open"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ssc-open-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
